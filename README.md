# Work Shift & Song Memorizer 🎵💼

Веб-приложение для учета рабочих смен и песен вне очереди с автоматическим расчетом заработка.

## 📋 Описание

Приложение предназначено для единого пользователя и позволяет:
- Вести учет рабочих смен с автоматическим расчетом стоимости
- Фиксировать песни, исполненные вне очереди
- Отслеживать общий заработок и текущий баланс
- Выполнять выплату зарплаты (обнуление текущего баланса)
- Хранить данные между перезапусками приложения

## ✨ Основные функции

### Расчет стоимости смен
- **Пятница/Суббота:** 4000 ₽
- **Остальные дни недели:** 3000 ₽
- **Праздничные дни (1-12 января):** 3000 ₽ (независимо от дня недели)

### Расчет стоимости песен
- **Фиксированная стоимость:** 1000 ₽

### Система выплат
- **За всё время:** отображает общий заработок (никогда не обнуляется)
- **Текущий баланс:** отображает заработок с последней выплаты
- **Кнопка "Забрать ЗП":** помечает все записи как оплаченные и обнуляет текущий баланс

## 🛠️ Технологический стек

### Backend
- **Java 21**
- **Spring Boot 3.2.1**
- **Spring Data JPA**
- **H2 Database** (файловое хранилище)
- **Lombok**
- **Maven**

### Frontend
- **HTML5**
- **CSS3** (современный дизайн в стиле "glassmorphism")
- **Vanilla JavaScript**

### Тестирование
- **JUnit 5**
- **Mockito**
- **Spring MockMvc**

## 📦 Установка и запуск

### Требования
- Java 21 или выше
- Maven 3.6+ (или использование встроенного Maven Wrapper)

### Шаги для запуска

1. **Клонирование репозитория:**
   ```bash
   git clone <repository-url>
   cd work-shift-memorizer
   ```

2. **Сборка проекта:**
   ```bash
   # Windows
   .\mvnw.cmd clean package

   # Linux/Mac
   ./mvnw clean package
   ```

3. **Запуск приложения:**
   ```bash
   # Windows
   .\mvnw.cmd spring-boot:run

   # Linux/Mac
   ./mvnw spring-boot:run
   ```

4. **Открыть в браузере:**
   ```
   http://localhost:8080
   ```

## 🧪 Запуск тестов

```bash
# Запуск всех тестов
.\mvnw.cmd test

# Запуск с отображением подробностей
.\mvnw.cmd test -X
```

### Покрытие тестами
- **Unit-тесты:** ShiftService (10 тестов), SongService (6 тестов)
- **Integration-тесты:** ShiftController (4 теста), SongController (4 теста), StatisticsController (7 тестов)
- **Всего:** 31 тест

## 📡 API Endpoints

### Управление сменами

#### GET `/api/shifts`
Получить список всех смен.

**Ответ:**
```json
[
  {
    "id": 1,
    "workerName": "Я",
    "date": "2026-01-20",
    "startTime": "00:00:00",
    "endTime": "23:59:59",
    "cost": 3000,
    "paid": false
  }
]
```

#### POST `/api/shifts`
Создать новую смену.

**Тело запроса:**
```json
{
  "workerName": "Я",
  "date": "2026-01-20",
  "startTime": "00:00:00",
  "endTime": "23:59:59"
}
```

**Ответ:** Созданная смена с автоматически рассчитанной стоимостью.

#### DELETE `/api/shifts/{id}`
Удалить смену по ID.

### Управление песнями

#### GET `/api/songs`
Получить список всех песен.

#### POST `/api/songs`
Добавить новую песню.

**Тело запроса:**
```json
{
  "title": "Название песни",
  "artist": "Вне очереди",
  "addedBy": "Гость"
}
```

#### DELETE `/api/songs/{id}`
Удалить песню по ID.

### Статистика

#### GET `/api/stats/earnings`
Получить статистику заработка.

**Ответ:**
```json
{
  "lifetimeEarnings": 25000,
  "currentBalance": 7000
}
```

#### POST `/api/stats/payout`
Выполнить выплату (пометить все записи как оплаченные).

## 🗄️ База данных

Приложение использует H2 Database с файловым хранилищем.

- **Путь к файлу БД:** `./data/memorizerdb`
- **H2 Console:** доступна по адресу `http://localhost:8080/h2-console`
- **JDBC URL:** `jdbc:h2:file:./data/memorizerdb`
- **Username:** `sa`
- **Password:** *(пусто)*

## 📁 Структура проекта

```
work-shift-memorizer/
├── src/
│   ├── main/
│   │   ├── java/com/example/memorizer/
│   │   │   ├── controller/          # REST контроллеры
│   │   │   ├── service/              # Бизнес-логика
│   │   │   ├── model/                # JPA сущности
│   │   │   ├── repository/           # Репозитории
│   │   │   └── MemorizerApplication.java
│   │   └── resources/
│   │       ├── static/               # Frontend (HTML/CSS/JS)
│   │       └── application.properties
│   └── test/
│       └── java/com/example/memorizer/
│           ├── controller/           # Интеграционные тесты
│           └── service/              # Unit-тесты
├── data/                             # База данных H2 (создается автоматически)
├── pom.xml
└── README.md
```

## 🎨 UI Features

- **Современный дизайн** с эффектами glassmorphism
- **Адаптивный интерфейс**
- **Плавные анимации** и hover-эффекты
- **Темная цветовая схема** с градиентами
- **Два счётчика:**
  - "За всё время" - общий заработок
  - "Текущий баланс" - с последней выплаты (подсвечен)

## 👤 Автор

**Pesterev Ivan**

## 📝 Лицензия

Этот проект создан в рамках учебного проекта.

## 🔧 Конфигурация

Настройки приложения находятся в `src/main/resources/application.properties`:

```properties
# Порт сервера
server.port=8080

# База данных H2
spring.datasource.url=jdbc:h2:file:./data/memorizerdb
spring.datasource.username=sa
spring.datasource.password=

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# H2 Console
spring.h2.console.enabled=true
```

## 🚀 Дополнительные возможности

- **JavaDoc на русском языке** для всех классов и методов
- **Comprehensive test coverage** с использованием JUnit 5 и Mockito
- **Автоматический расчет стоимости** на основе даты
- **Персистентное хранилище** данных между перезапусками

## 📞 Поддержка

При возникновении проблем:
1. Проверьте, что используется Java 21+
2. Убедитесь, что порт 8080 свободен
3. Проверьте права доступа к папке `data/`
4. Очистите и пересоберите проект: `.\mvnw.cmd clean package`

---

**Версия:** 1.0  
**Дата создания:** 2026-01-15  
**Последнее обновление:** 2026-01-20
